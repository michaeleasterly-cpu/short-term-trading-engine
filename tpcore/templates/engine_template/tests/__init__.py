"""Engine template — tests package marker.

Each engine MUST ship a ``<engine_name>/tests/`` directory; the SDLC
planner's readiness check requires this dir to exist before the
ECR-ADD can mutate ``tpcore.engine_profile._PROFILE``. Placeholder.
"""
from __future__ import annotations
