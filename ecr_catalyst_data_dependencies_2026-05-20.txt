# BLOCKED on planner extension (spec §7 follow-up to PR #191).
# As of 2026-05-20, ops/engine_sdlc/ecr.py defines
#   _MODIFY_KEYS = {"lab_dossier", "param_change", "gate_dsr", "gate_cred"}
# and the planner's attach_ecr_context only threads data_dependencies for ADD
# (planner.py:141). A MODIFY ECR carrying data_dependencies fails the
# _exactly_the_selected_action_fields validator with:
#   "field(s) ['data_dependencies', 'need'] not valid for action MODIFY"
# This ECR is staged as an artifact for the operator's manual MODIFY pass
# once _MODIFY_KEYS + planner._apply_modify are extended to thread
# data_dependencies (mirroring planner._apply_add lines 137-141).
#
# See docs/superpowers/audits/2026-05-20-engine-data-dependencies-accuracy.md
# §"Follow-up: extend ECR MODIFY to thread data_dependencies".
ECR
action:        MODIFY
engine:        catalyst
data_dependencies: prices_daily, sec_insider_transactions, earnings_events
need:          Accuracy fix — audit on 2026-05-20 found catalyst/backtest.py:292 reads platform.earnings_events (event_confirmation_mode="positive_beat_30d" gate, spec §2.2), but EngineProfile.data_dependencies omits it. Aligns with the pre-fold ENGINE_TABLES audit convention (backtest reads count: see vector earnings_events at backtest.py:269,847,1028 in the original 2026-05-16 file:line audit captured in PR #171's diff).
