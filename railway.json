{
  "$schema": "https://railway.com/railway.schema.json",
  "_comment": "MANDATORY: after editing this file, run `python ops/apply_railway_service_config.py --all` BEFORE the next push. Service-instance fields (deploy.<service>.{startCommand, cronSchedule, restartPolicyType, source.repo, watchPatterns}) live on Railway's service-instance config and are only updated via the GraphQL serviceInstanceUpdate mutation. Without the apply, push triggers a rebuild but the new container still runs with the OLD startCommand/cron/etc \u2014 you get a build-vs-runtime config drift that's painful to diagnose (build log shows the new buildCommand; runtime crashes anyway because startCommand wasn't propagated). The only field Railway actually reads from this file at build time is `build.buildCommand`. See docs/OPERATIONS.md \u00a71 'Build layout' and the project memory entry feedback_apply_after_railway_json.md.",
  "build": {
    "builder": "RAILPACK",
    "_build_note": "No `buildCommand` override on purpose. Railpack's Python provider auto-installs project deps from pyproject.toml into the mise-managed Python (`/mise/installs/python/<ver>/`), which IS copied to the runtime image. Setting a custom buildCommand REPLACES the auto-install with raw shell — the original `pip install -e .` did exactly that and used the build-container's root pip (not mise's), landing packages in a path the runtime image discards. Trust the default."
  },
  "deploy": {
    "sigma-scheduler": {
      "startCommand": "python sigma/scheduler.py",
      "cronSchedule": "0 22 * * MON-FRI",
      "restartPolicyType": "NEVER",
      "source": {
        "repo": "michaeleasterly-cpu/short-term-trading-engine"
      },
      "watchPatterns": [
        "**/*.py",
        "**/*.yaml",
        "pyproject.toml",
        "railway.json",
        "railpack.toml",
        ".python-version"
      ]
    },
    "reversion-scheduler": {
      "startCommand": "python reversion/scheduler.py",
      "cronSchedule": "0 22 * * MON-FRI",
      "restartPolicyType": "NEVER",
      "source": {
        "repo": "michaeleasterly-cpu/short-term-trading-engine"
      },
      "watchPatterns": [
        "**/*.py",
        "**/*.yaml",
        "pyproject.toml",
        "railway.json",
        "railpack.toml",
        ".python-version"
      ]
    },
    "vector-scheduler": {
      "startCommand": "python vector/scheduler.py",
      "cronSchedule": "0 22 * * MON-FRI",
      "restartPolicyType": "NEVER",
      "source": {
        "repo": "michaeleasterly-cpu/short-term-trading-engine"
      },
      "watchPatterns": [
        "**/*.py",
        "**/*.yaml",
        "pyproject.toml",
        "railway.json",
        "railpack.toml",
        ".python-version"
      ]
    },
    "ingestion-engine": {
      "startCommand": "python ops/ingestion_engine.py",
      "restartPolicyType": "ALWAYS",
      "source": {
        "repo": "michaeleasterly-cpu/short-term-trading-engine"
      },
      "watchPatterns": [
        "**/*.py",
        "**/*.yaml",
        "pyproject.toml",
        "railway.json",
        "railpack.toml",
        ".python-version"
      ]
    }
  }
}