# Sigma — Range Scalping Engine (daily timeframe)

Full specification to be added.

Engine consumes only `tpcore.interfaces.*` and approved data sources.
Trades execute exclusively through the Alpaca broker interface (paper → live
via the credibility-gated promotion flow).
